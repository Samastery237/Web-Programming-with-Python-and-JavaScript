# First: Defining a function to calculate the square of a number
def square(x):
    return x * x

for i in range(10):
    print(f"the square of {i} is {square(i)}")


# Second: importing the math module and using the sqrt function to calculate the square root of a number
def square(x):
    return x * x